---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { team } from '../../data/team';
---
<BaseLayout title="Our Team | IntelliSense Lab" description="Meet the IntelliSense Lab research team." headerMode="glass" bodyClass="archive-light-theme">
  <main class="publication-archive">
    <section class="publication-hero">
      <div class="container publication-hero-inner">
        <h1 class="publication-title" data-en="Our Team" data-zh="团队成员">Our Team</h1>
        <p class="publication-lede" data-en="Meet the researchers behind IntelliSense Lab" data-zh="认识 IntelliSense Lab 的研究团队成员">Meet the researchers behind IntelliSense Lab</p>
      </div>
    </section>

    <section class="section team-section">
      <div class="container team-grid">
        {team.map((member) => (
          <a class="team-card" href={`/our-team/${member.slug}/`}>
            <img class="team-img" src={member.img} alt={member.name} loading="lazy" />
            <div class="team-overlay">
              <p class="team-name" data-en={member.name} data-zh={member.nameZh ?? member.name}>{member.name}</p>
              <p class="team-role" data-en={member.role} data-zh={member.roleZh}>{member.role}</p>
              <p class="team-desc" data-en={member.desc} data-zh={member.descZh}>{member.desc}</p>
            </div>
          </a>
        ))}
        <a class="team-card alumni-card" href="/our-team/alumni/">
          <img class="team-img" src="/assets/team/alumni.jpg" alt="" loading="lazy" decoding="async" />
          <div class="team-overlay">
            <p class="team-name" data-en="Alumni" data-zh="校友">Alumni</p>
            <p class="team-role" data-en="Former members" data-zh="往届成员">Former members</p>
            <p class="team-desc" data-en="View the IntelliSense Lab alumni directory." data-zh="查看 IntelliSense Lab 校友名录。">View the IntelliSense Lab alumni directory.</p>
          </div>
        </a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .team-section {
    padding-top: 0;
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(4, 300px);
    gap: 36px;
    justify-content: center;
    margin: 0 auto;
  }

  .team-card {
    position: relative;
    aspect-ratio: 1;
    border-radius: 12px;
    overflow: hidden;
    cursor: pointer;
    text-decoration: none;
    border: 8px solid #fff;
    background: #fff;
    box-shadow: 0 0 0 1px #d9dcdf, 0 10px 28px rgba(17, 24, 39, 0.13);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .team-card:hover {
    transform: translateY(-4px);
    border-color: #fff;
    box-shadow: 0 0 0 1px #c9cdd1, 0 16px 38px rgba(17, 24, 39, 0.18);
  }

  .team-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 25%;
    transition: transform 0.3s ease;
  }

  .team-card:hover .team-img {
    transform: scale(1.05);
  }

  .team-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 16px;
    background: linear-gradient(to top, rgba(0,0,0,0.85) 0%, rgba(0,0,0,0.4) 50%, transparent 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .team-card:hover .team-overlay {
    opacity: 1;
  }

  .alumni-card .team-img {
    filter: grayscale(0.2) brightness(0.82);
    object-position: center;
  }

  .alumni-card .team-overlay {
    opacity: 1;
    background: linear-gradient(to top, rgba(5, 8, 12, 0.9) 0%, rgba(5, 8, 12, 0.22) 72%, transparent 100%);
  }

  .alumni-card .team-name {
    font-size: 22px;
    font-weight: 760;
  }

  @media (max-width: 1400px) {
    .team-grid {
      grid-template-columns: repeat(auto-fill, 320px);
    }
  }

  .team-name {
    margin: 0;
    color: #fff;
    font-size: 15px;
    font-weight: 600;
  }

  .team-role {
    margin: 4px 0 0;
    color: rgba(255, 255, 255, 0.7);
    font-size: 12px;
  }

  .team-desc {
    margin: 6px 0 0;
    color: rgba(255, 255, 255, 0.5);
    font-size: 11px;
    line-height: 1.5;
  }
</style>
